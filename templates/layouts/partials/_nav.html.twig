<nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="{{ path('home') }}">Hotel</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
             
                <ul class="navbar-nav mr-auto">
                {% if app.user %}
                    {% if is_granted('ROLE_RECEPTIONNISTE') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('chambre_index_RECE') }}">Chambres</a>
                    </li>
                    {% endif %}
                    {% if is_granted('ROLE_MANAGER') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('chambre_index_MANA') }}">Chambres</a>
                    </li>
                    {% endif %}
                    {% if is_granted('ROLE_MANAGER') %}
                    <li class="nav-item">
                        <a class="nav-link" href="/salarie">Salarié</a>
                    </li>
                    {% endif %}
                    {% if is_granted('ROLE_EMPLOYE_CHAMBRE') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('tache_My') }}">Tache</a>
                    </li>
                    {% endif %}
                {% endif %}
                </ul>
            

                <ul class="nav justify-content-end">
                    {% if app.user %}
                    <li class="nav-item">
                        <a class="btn btn-outline-danger" href="{{ path('app_logout') }}">logout</a>
                    </li>
                    {% else %}
                    <li class="nav-item">
                        <a class="btn btn-outline-success" href="{{ path('app_login') }}">log in</a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </nav>