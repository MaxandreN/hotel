{% extends 'layouts/base.html.twig' %}

{% block body %}
<div class="page">
<div class="archive">
    {% for user in users %}

        <article class="article">
            <h2 >{{user.nom}} {{user.prenom}}</h2>
            <div class="metadata">{{user.fonction.label}}</div>
            {% if user.nbTache %}
                <div class="metadata">nombre de tache(s) : {{user.nbTache}}</div>
            {% else %}
                <div class="metadata">aucune tache pour le moment</div>
            {% endif %}
            {% if user.libre == true %}
                <a href="/tache/edit/{{chambre.id}}/{{user.id}}" class="btn btn-success">assigner</a>
            
            {% elseif user.libre == false %}
                <a class="btn btn-outline-secondary">déjà assigner</a>
                <a href='/tache/del/{{user.tache}}' class="btn btn-outline-warning">Suprimer</a>
            {% endif %}
            <a href="../salarie/{{ user.id }}" class="btn btn-outline-primary">voir plus</a>


        </article>

    {% endfor %}


</div>
</div>
{% endblock %}
